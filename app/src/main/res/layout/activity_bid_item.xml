<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_bid_item"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.proiect.echipa478a.proiectandroid.ui.BidItemActivity">

    <TextView
        android:text="@string/item_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:id="@+id/bidItemName"
        android:textSize="20sp"
        android:textAlignment="center" />

    <ExpandableListView
        android:layout_width="match_parent"
        android:id="@+id/expandableListView"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/finishBidBtn"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/finishBidBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="onClick"
        android:text="@string/finish_bid"
        android:layout_below="@+id/bidItemName"
        android:layout_alignParentEnd="true" />

    <ImageView
        app:srcCompat="@mipmap/ic_launcher"
        android:id="@+id/itemImageView"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_height="150px"
        android:layout_width="150px" />
</RelativeLayout>
