<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_bidding_list"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.proiect.echipa478a.proiectandroid.ui.BiddingListActivity">

    <Button
        android:text="@string/sync"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/syncBtn"
        android:layout_alignParentTop="true"
        android:onClick="onClick"
        android:layout_alignParentEnd="true" />

    <ListView
        android:id="@+id/itemsListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/syncBtn"
        android:layout_alignParentStart="true" />

    <TextView
        android:id="@+id/bidItemsTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/bidding_items"
        android:textAlignment="center"
        android:textSize="20sp"
        android:layout_alignBaseline="@+id/syncBtn"
        android:layout_alignBottom="@+id/syncBtn"
        android:layout_alignParentStart="true" />
</RelativeLayout>
